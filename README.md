# this is local repo
Remote to local
1.git remote add origin link....
2.git remote -v   (verify remote)
3.git branch      (check branch)
4.git branch -m main   (change brnch name)
5.git commit -m "msg"
6.git push -u origin main