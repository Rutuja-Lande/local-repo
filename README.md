# this is local repo
Remote to local
1.git remote add origin link....<br>
2.git remote -v   (verify remote)<br>
3.git branch      (check branch)<br>
4.git branch -m main   (change brnch name)<br>
5.git commit -m "msg"<br>
6.git push -u origin main